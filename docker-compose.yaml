version: '3.9'

services:
  db:
    image: postgres
    volumes:
    - ./data/db:/var/lib/postgresql/data
    environment:
    - POSTGRES_USER=cs300
    - POSTGRES_PASSWORD=cs300
    - POSTGRES_DB=cs300
    ports:
    - "5432:5432"
